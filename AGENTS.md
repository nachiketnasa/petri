# Agent instructions

## Backend dependency management

Use `uv`.

- `uv sync` — install/sync dependencies
- `uv add <PACKAGE-NAME>` — add a dependency
- `uv run python <PYTHON-FILE>` — run a script in the project env

## Git

Commit code to git regularly.
